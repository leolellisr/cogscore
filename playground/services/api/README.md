# API service

Future FastAPI backend.

Responsibilities:

- Receive uploaded result bundles.
- Validate manifests and CSV files.
- Register runs.
- Create plotting jobs.
- Expose results, plots, jobs, and architectures through REST endpoints.
